apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: dp-server
spec:
  selector:
    matchLabels:
      app: dp-server
      role: master
      tier: backend
  replicas: 1
  template:
    metadata:
      labels:
        app: dp-server
        role: master
        tier: backend
    spec:
      containers:
      - name: master
        image: gcr.io/cloudpark-e6cc4/dp-server:v1
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 4000
---
apiVersion: v1
kind: Service
metadata:
  name: dp-server
  labels:
    app: dp-server
    role: master
    tier: backend
spec:
  ports:
  - port: 4000
    targetPort: 4000
  selector:
    app: dp-server
    role: master
    tier: backend